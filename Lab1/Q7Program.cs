using System;

namespace BaseKeywordHiddenApp
{
    class Program
    {
        /*
        static void Main(string[] args)
        {
            Console.WriteLine("DEMONSTRATING HIDDEN MEMBERS\n");

            // Create derived class object
            Dog dog = new Dog();
            dog.ShowAll();

            Console.WriteLine("\nDIRECT ACCESS TO HIDDEN MEMBERS");
            Console.WriteLine($"Accessing via Dog object - Name: {dog.name}");
            
            // Accessing base class hidden members using casting
            Animal animalRef = dog;
            Console.WriteLine($"Accessing via Animal reference - Name: {animalRef.name}");
        }
        */
    }
}